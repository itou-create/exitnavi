# CLAUDE.md — このリポジトリで作業するときの指示

## このプロジェクトは何か

駅の出口案内アプリ「出口ナビ」。公共交通オープンデータチャレンジ2026 への応募を想定。

**発端**：池袋駅で改札を出た後、案内板だけでは進む方向が分からなかった実体験。

**この第1段階のゴール**：**起点（どの路線のどのホームから来たか）を特定し、目的地に対して最適な出口を出す**ところまでを、実データで動かす。構内のステップ案内・地上ナビは第2段階なので、いまは作らない。

---

## 絶対に守ってほしい設計原則

この5つは企画の芯です。実装の都合で崩さないでください。

### 1. 現在地を「測らない」

屋内・地下でGPSは効きません。**測位して現在地を点で示すことはしない。**代わりに「次に何をするか」だけを順に示します。

やってよい測位は2つだけ：
- **どの駅にいるか**（数百m粒度）— Wi-Fi・基地局併用で地下でも取れることが多い
- **地上に出たかどうか**（accuracy の急改善で判定）

「改札のどちら側か」のような数m粒度は**取れない前提**で設計してください。

### 2. 推定を先に出し、訂正は1タップ

確実な判定ができないものは、**推定した答えを先に提示し、違えば1タップで直せる**形にします。ユーザーに最初から質問リストを見せない。

これは起点の特定・改札の内外、どちらにも適用します。

### 3. 起点はホームであって、駅ではない

GTFS-Pathways で経路を引くには `from_stop_id` が要ります。これは**駅IDではなくホームのID**。池袋は4事業者8路線、ホームは地上1Fから地下4Fまで散らばっており、**どのホームから来たかで最適な出口が変わります。**

「駅を特定したから案内できる」は誤りです。必ずホームまで決めてください。

### 4. 事前起動を必須にしない

「乗車中にアプリを開いておく」ことを**前提にしてはいけません**。改札を出て困った瞬間に開くのが需要の発生タイミングです。

乗車中に開いていた場合は**上乗せ**として扱います（`AppState.originSource` で分岐）。段階的な強化であって、必須条件ではありません。

### 5. 嘘の精度を表示しない

測位していないのに正確な現在地を描く、根拠のない所要時間を秒単位で出す、といったことはしない。**推定値には推定と分かる表示**をつけてください。ナビは一度外すとアプリごと信用されません。

---

## 技術方針

- **Vite + TypeScript + 素のDOM操作。フレームワークは入れない。**軽量Webであること自体が企画の主張の一部です（大手がUnity/3D/ビーコンで解こうとして「初回表示が遅い」「情報が多すぎる」という課題を残した領域を、逆に削って解く、という対比が資料の売りになっています）
- 状態管理ライブラリも不要。`src/state.ts` の単純なストアで足ります
- ビルド結果は静的ファイル。**GitHub Pages にそのまま置けること**を壊さないでください
- 依存を足したくなったら、まず足さずに書けないか考えてください

## ODPT API の扱い

**APIキーはまだありません。** そのため：

- `VITE_ODPT_TOKEN` が未設定なら**自動でモックにフォールバック**します（`src/services/odpt.ts`）
- **キーがなくても全画面が動く状態を維持してください。**これは開発の都合であると同時に、深夜や早朝など列車が走っていない時間帯に開発・デモするために必須です
- キーは https://developer.odpt.org/ で取得。`.env.local` に置き、**絶対にコミットしない**

### CORS について

ODPT API をブラウザから直接叩くと CORS で弾かれる可能性があります。**弾かれた場合は、勝手にプロキシサーバを立てないでください。**まず以下を試し、それでも駄目なら報告してください：

1. `vite.config.ts` の `server.proxy` で開発時だけ回避する（すでに設定済み）
2. 本番はビルド時に走行位置を取得する形にできないか検討する

サーバを増やすと GitHub Pages に置けなくなり、企画の前提が崩れます。

---

## いま実装済みのもの / これから作るもの

### 実装済み（動きます）
- 画面遷移とステート管理（`src/state.ts`, `src/ui/`）
- 駅・ホーム・出口のデータ定義（`src/data/stations.ts`）— **池袋・仙台・六丁の目・船橋**
- 対応駅の圏外・測位失敗時の駅選択画面＋デモモード（池袋着の想定でモック動作）
- 走行位置APIクライアント＋モック（`src/services/odpt.ts`）
- **起点推定ロジック**（`src/services/originGuess.ts`）← このプロジェクトの核心
- 出口の算出（`src/services/exitPicker.ts`）
- 位置情報の取得（`src/services/geo.ts`）

### 確認済みの事実（2026-08、ODPTカタログ）
- **仙台市交通局の ODPT 提供はバスのみ。**地下鉄（南北線・東西線）の走行位置は無い
- **JR東日本の走行位置（odpt:Train）は首都圏在来線のみ。**東北エリアは対象外
- → 仙台の2駅は全ホーム `trainLocationAvailable: false`。実APIでは問い合わせず、
  自動推定できないことをUIで正直に示す。手動選択が正規ルート

### 次にやること（優先順）
1. **池袋4事業者の走行位置API提供状況を実測する。**JR東・西武・東武・東京メトロのうち、どこが `odpt:Train` を出しているかで、起点推定の成否が決まります。ここが最優先
2. `src/data/stations.ts` の所要時間・出口位置を実測値に置き換える（現在は暫定値。`// TODO: 実測` を付けてあります。仙台・六丁の目は特に机上値なので現地実測が必要）
3. 新宿駅の追加（歩行空間ネットワークデータが整備されている数少ない駅）
4. 構内ステップ案内（第2段階）

---

## データの正しさについて

`src/data/stations.ts` の数値は**すべて暫定値です。**出口の座標、ホームから改札までの所要時間、階段の回数——現地で確認していません。

**この暫定値を「実測済み」であるかのように扱わないでください。**資料やREADMEに載せる際は暫定である旨を明記します。実測に置き換えたら `// TODO: 実測` のコメントを消してください。

## コミットについて

- `.env.local` をコミットしない
- `dist/` をコミットしない（GitHub Actions でビルドする想定）
